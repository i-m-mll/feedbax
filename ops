{"author":{"id":"1949ffc23f11a8cb4f8c42a45c9ad241b49570df9a18687489e9ea78d87c54a3"},"ops":[{"type":3,"timestamp":1779570039,"nonce":"50DSgvqh5ldXYYuTkxoDCNz9gLg=","message":"Retained-observable/loss/artifact implementation landed on `feature/graph-templates` at `fcf2b40`.\n\nConcrete contract now in code:\n- `GraphSpec.retained_observables` is the runtime probe/artifact surface. Legacy probe specs are not used by the worker path except through compatibility migration surfaces.\n- `lower_retention_plan` merges explicit retained observables, loss-required selectors, and artifact-required selectors into one plan with pathful validation.\n- Runtime selectors cover `port`, `edge`, `graph_output`, `recurrent_carry`, `state_path`, and `task_data` across backend and Studio types.\n- Loss terms preserve selector, target selector/value, retention, norm, and time aggregation; target comparisons and `all`/`mean`/`sum`/`final`/`range`/`custom` aggregation are executable before JIT. Segment-name aggregation remains blocked until timeline masks are explicitly lowered.\n- Worker manifests now write `retention_plan` and `retained_observables` artifacts separately from compact progress events.\n- Structured retained values are JSON materialized recursively, so retained graph outputs can be dataclass/PyTree mechanics states rather than only arrays.\n\nVerification covered selector validation, retention merging, target selector/value behavior, loss aggregation, provider validation, worker artifacts, and frontend objective lowering.","files":null}]}