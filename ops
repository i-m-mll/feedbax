{"author":{"id":"1949ffc23f11a8cb4f8c42a45c9ad241b49570df9a18687489e9ea78d87c54a3"},"ops":[{"type":3,"timestamp":1779570028,"nonce":"tf0u+K8AvsBSaHjTddVecuqkvKU=","message":"Implementation complete on `feature/graph-templates` at `fcf2b40`.\n\nWhat landed:\n- `GraphSpec` is now the worker execution boundary. `_run_training_real` delegates to `compile_training_run` / `run_training_graph`; the old worker bridge helpers, plant switch, synthetic trajectory runner, and `Network -\u003e SimpleStagedNetwork` lowering path were removed from runtime.\n- Retained observables are lowered through `feedbax.retained_observables`. Loss-required, explicit retained, and artifact-required selectors merge into one validated retention plan. Supported selector kinds are `port`, `edge`, `graph_output`, `recurrent_carry`, `state_path`, and `task_data`.\n- `LossTermSpec` is executable through lowered loss plans with selector/target_selector/target_value, retention, norm, and time aggregation. Frontend objective lowering preserves those fields, and backend/frontend time aggregation now includes `mean` and `sum`.\n- Graph execution now has `step_with_trace` selected tracing and threads recurrent carry through nested executable subgraphs, so serialized Network subgraphs keep their hidden carry instead of being reset by the parent graph.\n- Worker trainability is node/template declared, but the actual optimization mask selects only inexact array leaves inside declared trainable nodes. Mechanics/task/channel/static metadata are not accidentally optimized.\n- Worker artifacts now include compact event history plus manifest roles for `retention_plan` and `retained_observables`. Structured PyTrees/dataclasses are serialized recursively rather than assuming every retained value is an array.\n- Studio sends canonical graph/task/training/task-binding specs and runtime controls only; it no longer derives hidden_dim/network_type/plant behavior in the browser. Selector-keyed observables/outputs from trajectory events are stored, with legacy effector/target visualization kept only when derivable.\n\nTemplate inventory after this implementation:\n- Executable: `feedbax.templates.network` / Network Template; `feedbax.templates.simple_feedback` / Simple Feedback Loop.\n- Display-only: `feedbax.templates.cde_standard`, `feedbax.templates.cde_decay`, `feedbax.templates.cde_anti_nf`, `feedbax.templates.cde_hybrid_v9b`.\n\nResiduals intentionally outside this commit:\n- `a1036a6`: channel shape/prototype inference remains deferred as the explicit design issue.\n- Studio template product polish remains on `4f835cb`; the current branch exposes the backend/frontend template catalog and lists the final inventory there.\n- Segment-name time aggregation still requires timeline-mask lowering before it should execute in the generic loss compiler; this is validation-visible rather than silently guessed.\n\nVerification:\n- `uv run pytest tests/test_provider_contract.py tests/test_studio_workspace.py tests/test_graph.py tests/test_graph_templates.py tests/test_components.py tests/test_retained_observables.py tests/test_worker_execution.py -q` -\u003e 84 passed.\n- `uv run pytest tests/test_loss_service.py tests/test_provider_contract.py tests/test_retained_observables.py -q` -\u003e 83 passed.\n- `cd web \u0026\u0026 npm run test -- src/features/scenario/objectives.test.ts src/features/scenario/selectors.test.ts src/features/scenario/entities.test.ts src/features/scenario/projections.test.ts` -\u003e 19 passed.\n- `cd web \u0026\u0026 npm run build` -\u003e passed with the existing large chunk warning.\n- `uv run ruff check ...` and `git diff --check` passed.\n\nRLRMP migration note was also added on RLRMP `b41c940` and will be refreshed with this commit hash.","files":null}]}