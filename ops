{"author":{"id":"1949ffc23f11a8cb4f8c42a45c9ad241b49570df9a18687489e9ea78d87c54a3"},"ops":[{"type":3,"timestamp":1779166211,"nonce":"GzeVvylSJpNl2/Q7copKYUnRkZs=","message":"## Post-merge orientation: Studio has moved from Phase 4 vertical slice into product-depth follow-ups\n\nCurrent state as of the `develop` merges:\n\n- `971a994` merged the interaction-first Train/Evaluate collection UI slice from `integration/studio-rlrmp-run-collections`.\n- `0d82869` merged the Scenario Projection Workspace from `integration/studio-scenario-projection`: shared scenario entity registry, selection bus, `Graph / Workspace / Objectives` projections, inspector, objective authoring/lowering, task/mechanics bindings, stage protocol cleanup, and RLRMP analysis/report linkage.\n- The stage/scenario badge issue `7044904` is stale bookkeeping now; the placement was fixed in the scenario merge but the issue stayed open because it was not attached to the auth request.\n- Full Vitest still has a suite-hygiene blocker: `e034af5` now tracks converting `web/src/data/rlrmp-part1.test.ts` from a standalone harness into real Vitest tests.\n\nThe roadmap issue is closed/merged, but it still works as the cross-repo orientation ledger. The remaining work is no longer \"finish Phase 4\". It is product hardening and deeper contracts on top of the merged Studio foundation.\n\nRecommended next order:\n\n1. Do immediate cleanup: treat `7044904` as stale/resolved bookkeeping and fix `e034af5` so full Vitest is reliable again.\n2. Implement `85cec2c` next: a state/selector browser for structured object and array internals. The merged UI has a quick substate/path field and objective selector plumbing, not real runtime state-tree introspection for paths like `mechanics.effector.position`, indexed arrays, final velocity, or slices.\n3. Then implement `e5a1e6c`: task-aware metric columns from objective, metric, analysis, task template, or imported-manifest specs rather than RLRMP-shaped hardcoded table fields.\n4. After that, choose the next large track based on product goal: `e57ae1f` for real JAX training backend/lowering if Studio should feel executable, or `c6c6da0` / `f68cf66` / `e33f487` for provider/manifest/indexing depth if Mandible and agent interop robustness is the priority.\n5. Keep `0c46f69` open as the bottom-pane pipeline/workspace pressure surface: collection transforms, stage-owned specs, execution lineage, analysis/report flow, readiness/errors/logs/artifacts, and provenance are only partially satisfied by the merged slices.\n6. Keep `6116155` and related component/palette issues as the later canvas-taxonomy track: typed subgraphs, acausal/causal/Penzai/composite/analysis contexts, palettes, compatibility rules, and specialized editors.\n\nOperational note for future agents: do not start with more broad visual polish. First make the selector/state-browser and metric-source contracts real, because they complete the objective/scenario authoring work that is now visible in the app.\n\n---\nCo-Authored-By: Codex (GPT-5) \u003ccodex@openai.com\u003e","files":null}]}