{"author":{"id":"1949ffc23f11a8cb4f8c42a45c9ad241b49570df9a18687489e9ea78d87c54a3"},"ops":[{"type":3,"timestamp":1779461409,"nonce":"ojQ4+pEs9vD2GDCY+Yroi89tnqs=","message":"Implementation update after browser annotation pass:\n\n- Root cause was not one special edge case; schema propagation had two missing boundary paths. Parent node ports now read child subgraph boundary schemas, and child graph input/output boundary ports now participate as real dimension sources/consumers for internal nodes.\n- The delayed-reach task-data mux path now resolves by graph role/topology instead of assuming a node named `task_mux`, so saved graphs with `mux.output -\u003e network.input` still get default task-data bindings.\n- `target_position` is now schemaed as the full forceless Cartesian target state sample `[pos, vel]` with width 4, not only `.pos` width 2. With hold/go and target-shown cues this makes the default mux width 6. Saved stale task-data schemas are refreshed through `ensureTaskBindingSpec` while preserving existing value settings.\n- Live IAB check now shows the network child `cell.input_size` auto-synced to 6 and `hidden_size` remaining 100.\n\nVerification: `pnpm vitest run src/features/schema/project.test.ts src/features/scenario/taskBindings.test.ts src/features/scenario/taskTimeline.test.ts src/features/graph/dynamicPorts.test.ts src/stores/workspaceStore.test.ts`; `pnpm build`; `uv run ruff format feedbax/studio_schema.py tests/test_provider_contract.py \u0026\u0026 uv run ruff check feedbax/studio_schema.py tests/test_provider_contract.py`; `uv run pytest tests/test_provider_contract.py -k studio_schema`.","files":null}]}