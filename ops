{"author":{"id":"1949ffc23f11a8cb4f8c42a45c9ad241b49570df9a18687489e9ea78d87c54a3"},"ops":[{"type":3,"timestamp":1779167051,"nonce":"1/Qho2xL0YsV46CBaot4QxsaPGo=","message":"## Implementation spec before editing\n\nScope is intentionally narrow: convert the existing structural checks in `web/src/data/rlrmp-part1.test.ts` into ordinary Vitest tests without changing the template semantics.\n\nPlan:\n\n1. Replace the custom `passed` / `failed` counters, `assert`, `assertEq`, `section`, console output, and `process.exit(1)` with `describe`, `it`, and `expect`.\n2. Keep `validatePage()` as a helper, but make it use assertions directly and keep error messages implicit in the assertion context rather than printing a custom report.\n3. Group assertions around the existing user-facing template invariants: snapshot shape, page names, graph structure, measure fidelity, frequency-response parameters, unit-preference variants, and transform presence.\n4. Verify the focused file and then the full Vitest run before relying on test output for the product slices.\n\nThis should be a pure test-hygiene change. No app UX or data model behavior belongs in this issue.\n\n---\nCo-Authored-By: Codex (GPT-5) \u003ccodex@openai.com\u003e","files":null}]}