{"author":{"id":"1949ffc23f11a8cb4f8c42a45c9ad241b49570df9a18687489e9ea78d87c54a3"},"ops":[{"type":3,"timestamp":1779307447,"nonce":"qE8f0fcvctGXiUtSybVvw8PeNa8=","message":"Post-schema/real-worker review note after merges `af6e403` and `64dafe8`.\n\nGemini's external review correctly called out that the Studio worker is still a config-driven bridge, not a full graph/spec lowering path. The recent `e57ae1f` slice improved the situation by removing silent fallback architecture and requiring real Studio payloads, but the worker still has hardcoded dispatch surfaces:\n\n- `feedbax/web/worker/app.py::_extract_graph_params` uses explicit `CELL_MAP`, `NONLINEARITY_MAP`, and mechanics type allowlists;\n- `_run_training_real` still manually constructs the supported plant/controller path and fixed action/observation assumptions;\n- `PointMass8MuscleRelu` is intentionally rejected until explicit muscle wiring exists.\n\nThis should stay under this umbrella rather than becoming a duplicate broad issue. Clean next slice: replace the worker bridge dispatch with graph/spec lowering that derives plant, controller, action size, observation schema, and wiring from declared graph topology/registry metadata. That should build on the schema registry, task protocol roles, and compact task-param work now on `develop`.","files":null}]}