{"author":{"id":"1949ffc23f11a8cb4f8c42a45c9ad241b49570df9a18687489e9ea78d87c54a3"},"ops":[{"type":3,"timestamp":1774312910,"nonce":"zyTiHPalHhRwN4QSDDbnDmvm3/g=","message":"**Spec — implementation plan:**\n\n1. **normalizeUiState reversal persistence**: After tracing the full save/load path (graphStore -\u003e API client -\u003e FastAPI backend -\u003e Pydantic model -\u003e JSON file -\u003e back), the current code in normalizeUiState() is actually correct for the basic case. The `if (!node_states[nodeId])` guard on line 925 only creates defaults for nodes absent from the loaded state, and line 937 uses `??` which preserves existing `true` values. However, the TypeScript NodeUIState type makes `reversed` optional (`reversed?: boolean`), which creates fragility. Fix: make the merge semantics explicit with a spread of defaults UNDER loaded values, and add a comment documenting the invariant.\n\n2. **Remove Analysis DAG pill**: Delete the Panel at lines 168-173 in AnalysisCanvas.tsx.\n\n3. **DataSourceNode restyle**: Replace the label-based header with just a Database icon, removing the code-like \"AnalysisInputData\" text. Keep the muted style and port labels.\n\n4. **RunSelector chevron overlap**: Add `pr-10` right padding to the RunSelector container in BottomShelf.tsx so dropdowns don't overlap with the Divider chevron circles (which are positioned at `right-3` absolute).\n\n5. **CLAUDE.md conventions**: Add No-jitter and No-volatility under a new \"UI Conventions\" section.","files":null}]}