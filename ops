{"author":{"id":"1949ffc23f11a8cb4f8c42a45c9ad241b49570df9a18687489e9ea78d87c54a3"},"ops":[{"type":3,"timestamp":1779564502,"nonce":"KzzT3+uKhGw1nCWPTVRXkoLqbwI=","message":"## Investigation/spec checkpoint before continuing implementation\n\nI paused after the initial audit and before broadening the patch. Four read-only subagents audited distinct surfaces: graph/runtime, worker lowering, legacy staged/simple-feedback classes, and probe/retention semantics. The findings agree on the implementation boundaries below.\n\nCurrent confirmed state:\n\n- `GraphSpec` is already the durable JSON-compatible graph shape and already carries `nodes`, `wires`, boundary bindings, optional `subgraphs`, `taps`, and recurrent wire metadata.\n- `WireSpec.temporality` and `recurrent_initializer` are persisted and validated, but runtime `Graph` only used explicit `cycle_init` or source state-derived outputs; initializer metadata was not yet interpreted.\n- Studio/Python normalization already migrates `SimpleStagedNetwork` payloads to authoring `Network` nodes with generated internal GRU/LSTM/readout subgraphs.\n- The hard contradiction with this umbrella is `spec_to_graph(Network)`: it reads the internal subgraph only to recover params and then rebuilds a `SimpleStagedNetwork`.\n- The worker has a graph-derived bridge for task bindings and action-path validation, but still extracts GRU/LSTM/readout params and builds/calls `SimpleStagedNetwork` internals. It also appears to accept LSTM while the rollout shape is effectively GRU-only.\n- `SimpleFeedback` is already a `Graph` subclass, but still acts as a privileged constructor/view layer because it emits `SimpleFeedbackState` and downstream loss/analysis code assumes `states.mechanics`, `states.net`, and `states.efferent`.\n- Probes currently exist as fragmented `TapSpec`, `BarnacleSpec`, untyped `scenario.probe_specs`, implicit port probes, and selector targets. They are not yet retention-aware observables.\n\nImplementation decisions for this branch:\n\n1. Add a canonical Python graph-template module for built-in template/spec factories. Start with executable `Network` and `Simple Feedback Loop` templates, with metadata suitable for later Studio template catalog exposure.\n2. Make runtime recurrence metadata real by resolving supported recurrent initializers (`zeros`, then explicit constants) in `Graph.initial_cycle_port_values`; explicit `cycle_init` remains an override.\n3. Change `spec_to_graph(Network)` to instantiate and execute the serialized internal subgraph as a `Graph`, not lower it to `SimpleStagedNetwork`. Keep `_build_network`/`_params_from_network_subgraph` only as legacy/worker compatibility helpers while needed.\n4. Make leaf GRU/LSTM graph components state-backed enough for state views and initial outputs, so recurrent subgraphs can run without a hidden composite class.\n5. Keep the real worker as an explicit optimized bridge for this branch. For `Network` specs, move the controller step toward calling the instantiated graph subcomponent; for legacy `SimpleStagedNetwork`, keep a compatibility path but reject overclaimed shapes such as LSTM unless the bridge truly handles them.\n6. Add typed retained-observable specs and retention policy fields now, but do not pretend the full streaming/window/trajectory artifact pipeline is done until execution, loss lowering, and Studio Data projection consume that plan.\n\nDeferred only because it is a larger sibling contract, not because it is out of the umbrella:\n\n- Full arbitrary plant/mechanics execution from `GraphSpec` in the worker. Current mechanics templates still need enough serializable plant/body/topology data before generic graph execution can replace the worker plant dispatch.\n- Complete loss/objective lowering from retained observables. This is sequenced after the retained-observable spec exists and should stay cross-linked to `0e1cd8c`.\n- Downstream RLRMP checkpoint/model migration remains in RLRMP `b41c940`; Feedbax should provide compatibility/import hooks and graph specs, not promise blind `.eqx` compatibility.\n\nI have already started small edits on the branch before writing this comment. I am stopping broad implementation until this issue trail is in place, then will continue with the above order and split coding work to workers where file ownership is separable.","files":null}]}