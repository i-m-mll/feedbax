{"author":{"id":"1949ffc23f11a8cb4f8c42a45c9ad241b49570df9a18687489e9ea78d87c54a3"},"ops":[{"type":3,"timestamp":1779167837,"nonce":"C1hQrdikQNkGbtO6MDxldvccyIs=","message":"## Result: first task-aware metric table slice on feature/studio-selector-metrics\n\nImplemented the first table refactor so Train/Evaluate rows are no longer rendered from a fixed RLRMP-shaped column list.\n\nWhat changed:\n- Training run summaries now expose a metric-value map derived from manifest metadata, including numeric fields and `{mean, sd}` metric objects.\n- Added a metric-column helper that chooses visible table columns from `scenarioMetricSpecs(workspace)` plus actual row compatibility.\n- Train and Evaluate tables now keep fixed interaction columns for selection, run identity, progress, checkpoint, and details, while metric columns are derived from the metric catalog.\n- Sorting now uses metric IDs such as `final_validation_loss`, `within_cell_velocity_rmse_m_per_s`, `peak_velocity_m_per_s`, and `hold_drift_mm` rather than hardcoded table keys.\n- The metric side panel now shows unique metric sources instead of repeating per-run manifest paths.\n\nUX boundary:\n- The seeded RLRMP example still shows loss, velocity RMSE, peak velocity, and hold drift because those are the compatible imported/analysis metrics available. The table no longer assumes those are universal columns.\n- Column configuration/pinning/filter-builder UX is still a later product slice; this branch creates the spec-driven column substrate first.\n\nVerification:\n- Added tests for per-run metric maps and metric-column derivation from a non-hardcoded metric spec.\n- Full `cd web \u0026\u0026 npx vitest run` passed: 14 files, 114 tests.\n- `cd web \u0026\u0026 npm run build` passed with only the existing large Plotly chunk warning.\n- Browser smoke on the seeded RLRMP example verified the Train/Evaluate tables render metric columns from derived metric specs with units and sort controls.\n\n---\nCo-Authored-By: Codex (GPT-5) \u003ccodex@openai.com\u003e","files":null}]}