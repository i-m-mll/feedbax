{"author":{"id":"1949ffc23f11a8cb4f8c42a45c9ad241b49570df9a18687489e9ea78d87c54a3"},"ops":[{"type":1,"timestamp":1773064741,"nonce":"yHSLRZESG0pgO5PD/RZSl2WLTWg=","title":"Cloud training dispatch: wire training_spec/task_spec, checkpoint download, startup script","message":"Phase 1 cloud training dispatch implementation per CLOUD_TRAINING_DISPATCH_SPEC.md.\n\n## Gaps being closed\n\n1. **training_spec forwarding**: Propagate optimizer type/params from UI through api/training.py → services/training_service.py → worker/client.py → worker/app.py. Build optimizer from spec in worker instead of hardcoding AdamW.\n2. **task_spec forwarding**: Propagate n_reach_steps, effort_weight from task spec through same chain.\n3. **Checkpoint weights**: Serialize trained controller with eqx.tree_serialise_leaves after training. Add /checkpoint/download worker endpoint. Proxy through Studio backend. Frontend download button.\n4. **Startup script**: Replace PyPI feedbax install with git-install from develop branch. Add feedbax_install_cmd field to InstanceConfig/LaunchRequest.\n\n## Critique fixes to apply (blocking, from Codex + Gemini review)\n- cfg mutation (effort_weight) must precede _train_step JIT definition\n- training_spec must be added to _Job dataclass; _run_training wrapper must thread it through\n- tempfile.mktemp() → mkstemp() in Studio API proxy\n- FileResponse import missing from api/training.py\n- jax.block_until_ready(controller) needed before eqx.tree_serialise_leaves\n- Eager checkpoint pull to Studio backend (not deferred to separate download call) to survive GCP preemption\n- Default to GPU machine type in startup script (n1-standard-4 is CPU-only; jax[cuda12] needed)\n\nSpec: docs/CLOUD_TRAINING_DISPATCH_SPEC.md\nRelated: e57ae1f (Phase 6 wiring)","files":null}]}