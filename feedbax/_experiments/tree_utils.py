"""Backward compatibility shim - tree_utils is now in feedbax.tree_utils."""
from feedbax.tree_utils import *
from feedbax.tree_utils import (
    swap_model_trainables,
    subdict,
    dictmerge,
    map_kwargs_to_dict,
    ldict_verbose_label_fn,
    ldict_label_only_fn,
    tree_level_types,
    tree_level_labels,
    print_ldict_tree_summary,
    swap_adjacent_ldict_levels,
    move_ldict_level_above,
    ldict_level_to_top,
    ldict_level_to_bottom,
    getitem_at_level,
    rearrange_ldict_levels,
    ldict_level_keys,
    check_nan_in_pytree,
    tree_map_with_keys,
    tree_subset_ldict_level,
    flatten_with_paths,
    index_multi,
    pp,
    pp2,
)
